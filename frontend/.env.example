# Copy to frontend/.env.local for local dev (usually not needed — the Vite dev
# proxy handles /api). In production Render injects this at build time.

# Absolute base URL of the deployed API, including the /api/v1 prefix.
VITE_API_BASE_URL=https://ohas-api.onrender.com/api/v1
